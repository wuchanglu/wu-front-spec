#!/usr/bin/env sh
# 加载当前目录下的 `h` 文件，该文件通常包含一些配置或函数，用于设置环境变量或执行其他必要的操作
. "$(dirname "$0")/h"
# 使用 `npx` 命令运行 `commitlint` 工具，检查提交信息是否符合规范
# `--edit` 参数表示在编辑器中打开提交信息文件进行编辑
# `$1` 是传递给脚本的第一个参数，通常是提交信息文件的路径
npx commitlint --edit $1
